{
  "name": "test-work-playwright",
  "version": "1.0.0",
  "description": "Test work for a SDET position",
  "main": "index.js",
  "scripts": {
    "test:regression": "npx playwright test --config=playwright-e2e-tests.config.ts --project='E2E testing with chromium' --headed",
    "test:regression-with-reports": "npx playwright test --config=playwright-e2e-tests.config.ts --project='E2E testing with chromium' --headed"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "prettier": "^2.5.0",
    "winston": "^3.10.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.36.1",
    "allure-commandline": "^2.23.0",
    "allure-playwright": "^2.4.0"
  }
}
